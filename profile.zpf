driver=$(basename "$(readlink "/sys/class/net/$1/device/driver")")
if [ "$driver" = sfc ]; then
  speed=`(cat /sys/class/net/$1/speed 2>/dev/null) || echo 0`
  if [ $speed -le 10000 ]; then
    zf_set ctpio_mode ct
  else
    zf_set ctpio_mode sf
  fi
else
  zf_set ctpio_mode ct
fi
